import {
	Table,
	TableBody,
	TableCell,
	TableHead,
	TableHeader,
	TableRow,
} from "@/Components/atoms/table";

type DataTableProps = {
	children?: React.ReactNode; // Para aceptar children
};

export function DataTable() {
	return <div>hoa</div>;
}
